"""helios.pipelines — peer pipeline stubs for Stage 0.

D-pipe, G-pipe, L-pipe are null stubs gated by VCLFlag. Full implementations follow
Stage 2 (D-pipe), Stage 4 (G-pipe), Stage 5 (L-pipe) per the execution plan.
"""
